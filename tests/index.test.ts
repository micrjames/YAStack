const { YAStack } = require("../YAStack");

   //  add(element: any);
   //  remove();
   //  peek();
   //  isEmpty();
describe("A stack.", () => {
   describe("Basic operations.", () => {
	  test.todo("Should not be empty.");
	  test.todo("Should add the item.");
	  test.todo("Should peek the item.");
	  test.todo("Should be of given size.");
	  test.todo("Should remove the item.");
   });
   describe("That is iterable.", () => {
	  test.todo("Should be a stack of 5 items.");
	  test.todo("Should output a stack of the 5 items contained.");
   });
});
